version: 2

jobs:
  test:
    docker:
      - image: pnedelko/circleci-node-8-awscli:1.0.0
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: Installing dependencies
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run:
          name: Testing
          command: npm test
      - run:
          name: Uploading to S3
          command: aws s3 sync ./dist s3://elasticio-cdn-eu/vendor/jsonata-moment/testing-`npm view @elastic.io/jsonata-moment version`

  publish:
    docker:
      - image: circleci/node:8
    steps:
      - checkout
      - restore_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
      - run:
          name: Installing dependencies
          command: npm install
      - save_cache:
          key: dependency-cache-{{ checksum "package-lock.json" }}
          paths:
            - ./node_modules
      - run:
          name: Creating .npmrc with authToken
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/.npmrc
      - run:
          name: Publishing to npm
          command: npm publish
workflows:
  version: 2
  build:
    jobs:
      - test:
          filters:
            branches:
              ignore: master
      - publish:
          filters:
            branches:
              only: master
